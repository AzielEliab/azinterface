# AZInterface

Public identity: **Aziel Eliab** only.

This is a custodial operating environment (AIH-WP-1.0). Interface is
CUSTODY. It is not AZHub, not a vault dump, and not a remote wipe
service.

Apps load in five pre-locked page cycles:
`OFF → integrity → ON → FULL SHUTDOWN → MEMORIAL`.
One step only. No skip. Do not invent cloud-asleep availability.

Scorched Earth is a local stub/advisory only. Never remotely wipe
user devices. Never store a username. Never serve vault contents.

Agent path is the one FragGate door:
`POST https://aziel-runtime.vibelock.workers.dev/v1/fraggate/call`
with `slug=azinterface`. AZHub is separate software under that door.
Not a second Interface MCP. Human UI stays local / Worker.

Forks are welcome and always allowed. Apache-2.0.
